1. Line 12 will print the value of i when the for loop is done running. This is because the variable i was declared as a var and can be accessed anywhere within the discountPrices function. ``3`` will be printed since the for loop runs a total of 3 times.
2. Line 13 will print the value of discountedPrice after the last iteration of the for loop. This is because discountedPrice was declared as a var and can be accessed outside the for-loop scope that it was declared within.
3. Line 14 will print the value of finalPrice after the for loop before it has finished running and it will hold the value that it was given during that last iteration of the for-loop.
4. The function will return an array of prices after the discount has been applied to their price. This is because during each iteration of the for-loop, we update the ``discounted`` array with the discounted value and return that array of discounted prices at the end of the function.
5. During the execution of line 12, it will cause an error because it cannot access the variable ``i`` since it was declared in a different scope than it is trying to be accessed in. So the variable ``i`` doesn't exist in the same scope that line 12 resides in.
6. Line 13 will also cuase an error since it is trying to access the ``discountedPrice`` variable that was delcared in a different scope than where line 13 is. The variable does not exist in line 13's scope. 
7. The value of ``finalPrice`` will be printed by line 14 because finalPrice was declared in the same scope that line 14 is in. The value that will be printed is the value of finalPrice at the last execution of the for loop.
8. The function will return an array of prices after the discount has been applied to their price. This is because during each iteration of the for-loop, we update the ``discounted`` array with the discounted value and return that array of discounted prices at the end of the function.
9. Line 11 will cause an error because it cannot access the variable ``i`` since it was declared in a different scope than it is trying to be accessed in.
10. Line 12 will print the value of ``length`` since it was declared within the same scope that line 12 resides in. It will print the value ``3`` since the length of the prices array that is being passed in is of length 3.
11. This function will return an array of prices after the discount has been applied to their price. This is because during each iteration of the for-loop, we update the ``discounted`` array with the discounted value and return that array of discounted prices at the end of the function.
12. (a) student.name (b) student["Grad Year"] (c) student.greeting() (d) student["Favorite Teacher"].name (e) student.courseLoad[0]
13. (a) 32 since 3 is a string the '+' symbol is used for string concatenation so we are concatenating 2 which was changed from an int to a string. (b) 1 because '-' only works with numbers so we change 3 from an string to an int and then subtract 1. (c) 3 since null is represented as 0 in numeric operations, so we do 3 + 0 which equals 3. (d) 3null since 3 is a string and we are concatenating with the '+' symbol so we concatenate the string 3 and the string 'null'. (e) 4 since true is converted to 1 in a numeric conversion so we end up adding 3 and 1 which gives us 4. (f) 0 since false and null are both converted to 0 and then we add 0 + 0 which gives us 0. (g) 3undefined since 3 is a string and undefined gets converted into a string and concatenated with 3. (h) NaN because '-' is only allowed with numbers which changes 3 from a string to an int but undefined cannot be converted to a number so we end up with NaN.
14. (a) true since 2 is converted from a string to a number and then compared with 1 which is true. (b) false since they are both strings, they are compared lexicographically and 2 does not come before 1 in alphabetical order. (c) true since the string 2 is converted to an int and then they are compared. (d) false since the 3 equal signs don't allow type conversion so false is returned since they are of different types. (e) false because true is converted to its numeric representation which is 1 and then checks if it is equal to 2 which is false. (f) true because Boolean(2) evaluates to true so true == true which is true.
15. == converts values to numbers first and then checks equality. === does not convert values and checks equality without type conversion.
16. Found in part2-question16.js
17. The result of the function call will be an array of values [2,4,6]. This is because when we call modifyArray, we give it an array of values along with another function called doSomething that simply takes the parameter it is given and multiplies the number by two. modifyArray first creates an empty array, then for every number in the array, uses the callback to call doSomething(num) with the value at that location in the array and pushes it to the newArr. At the end of the for loop, newArr is returned with the updated values. 
18. Found in part2-question18.js
19. The output of the code is 1 4 3 2 where each number is on a new line. 